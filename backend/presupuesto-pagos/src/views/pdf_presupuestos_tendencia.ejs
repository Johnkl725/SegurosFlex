<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Tendencia Mensual de Presupuestos</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 20px; }
    h1 { text-align: center; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th, td { border: 1px solid #333; padding: 8px; text-align: center; }
    th { background-color: #eee; }
  </style>
</head>
<body>
  <h1>Tendencia Mensual de Presupuestos</h1>
  <table>
    <thead>
      <tr>
        <th>Mes</th>
        <th>Total Presupuestos</th>
        <th>Promedio Presupuesto</th>
      </tr>
    </thead>
    <tbody>
      <% datos.forEach(function(item) { %>
      <tr>
        <td><%= item.mes instanceof Date ? item.mes.toISOString().substring(0,10) : item.mes.substring(0,10) %></td>
        <td><%= item.total_presupuestos %></td>
        <td><%= item.promedio_presupuesto %></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
</body>
</html>
